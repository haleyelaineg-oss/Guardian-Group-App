import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { supabase } from '../../lib/supabase.js';
import { formatDate } from '../../utils/format.js';
export default function DashboardPage() { const [data, setData] = useState(null); useEffect(() => { Promise.all([supabase.from('events').select('id,title,starts_at,status').gte('starts_at', new Date().toISOString()).neq('status', 'cancelled').order('starts_at').limit(6), supabase.from('tasks').select('id,title,due_date,status').neq('status', 'done').order('due_date', { ascending: true, nullsFirst: false }).limit(8), supabase.from('speaking_engagements').select('id', { count: 'exact', head: true }).in('status', ['selected', 'contracting', 'planning', 'ready']), supabase.from('training_engagements').select('id', { count: 'exact', head: true }).in('status', ['scheduled', 'planning', 'ready'])]).then(([events, tasks, speaking, trainings]) => { if (events.error || tasks.error || speaking.error || trainings.error) throw events.error || tasks.error || speaking.error || trainings.error; setData({ events: events.data || [], tasks: tasks.data || [], speaking: speaking.count || 0, trainings: trainings.count || 0 }); }).catch((err) => alert(err.message)); }, []); if (!data) return <p className="empty-hint">Loading dashboard...</p>; return <div className="view active"><div className="view-header"><h1 className="view-title">Dashboard</h1></div><div className="stats-grid"><div className="stat-card accent"><div className="stat-value">{data.events.length}</div><div className="stat-label">Upcoming Events</div></div><div className="stat-card"><div className="stat-value">{data.tasks.length}</div><div className="stat-label">Open Tasks</div></div><div className="stat-card"><div className="stat-value">{data.speaking}</div><div className="stat-label">Active Speaking</div></div><div className="stat-card"><div className="stat-value">{data.trainings}</div><div className="stat-label">Active Trainings</div></div></div><div className="fields-grid"><section><div className="detail-section-title">Upcoming Events</div>{data.events.length ? data.events.map((event) => <Link className="task-row" key={event.id} to={`/admin/events/${event.id}`}><span className="task-row-title">{event.title}</span><span className="task-row-due">{formatDate(event.starts_at.slice(0, 10))}</span></Link>) : <p className="empty-hint">No upcoming events.</p>}<Link to="/admin/calendar">View calendar →</Link></section><section><div className="detail-section-title">Open Tasks</div>{data.tasks.length ? data.tasks.map((task) => <Link className="task-row" key={task.id} to="/admin/tasks"><span className="task-row-title">☐ {task.title}</span><span className="task-row-due">{task.due_date ? formatDate(task.due_date) : 'No due date'}</span></Link>) : <p className="empty-hint">No open tasks.</p>}<Link to="/admin/tasks">View tasks →</Link></section></div></div>; }
